import type { NextConfig } from "next";

const nextConfig: NextConfig = {
 

  /* config options here */
};

export default nextConfig;
// You do not need next.config.js domains if the images are local (in your public folder). You only need config if the URL starts with https://.